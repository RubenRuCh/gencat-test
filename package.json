{
  "name": "test-gencat-playwright",
  "version": "1.0.0",
  "description": "Technical test using playwright",
  "main": "index.js",
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "scripts": {
    "test": "jest --testTimeout=10000 --force-exit --silent --verbose",
    "test:headless": "cross-env NODE_ENV=headless jest --testTimeout=10000 --force-exit --silent --verbose"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "expect-playwright": "^0.3.4",
    "jest": "^26.6.3",
    "jest-playwright-preset": "^1.5.2",
    "playwright": "^1.11.0",
    "playwright-core": "^1.11.0"
  },
  "dependencies": {
    "@babel/core": "^7.14.3",
    "@babel/preset-env": "^7.14.2",
    "babel-jest": "^26.6.3",
    "cross-env": "^7.0.3"
  }
}
